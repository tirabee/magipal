# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Updater signing keys. The real key lives in ~/.tauri/ and is never in the repo,
# but if one is ever copied here by accident it must not be committable: leaking
# it lets anyone push a signed "update" to every Magipal install.
*.key
*.pem
!*.key.pub

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Built release artifacts — published to GitHub Releases, not committed.
release/
